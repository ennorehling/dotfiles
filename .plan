tolua++ debian package has a bug in variable.lua (not dealing with outside)
install luarocks
install lunit and write unit tests for atlantis

Done:
. killing it_find / rewrite with rt_find
. jsonconf reads items
. new_resourcetype is awful
. eliminate new_itemtype()
. rt_get_or_create must rt_register! (test.test.c)
. why is appearence not in item_type ?
. eliminate new_race[] array
. kill static variables (^\s+static)
. optimize get_race()
. strings in jsonconf
. it_create replaces new_itemtype
. requirement.recycle is stupid, never used

To Do:
. jsonconf race.speed
. get_param_* cache breaking
. jsonconf reads items with attributes
. replace rt_find with get_resourcetype where we can
. kill superfluous R_* enums
. invert RCF_WALK, it is a shitty default
. other handlers besides nrmessage() to render localization
. register_function/get_function to support Lua callbacks
. msg_message deals poorly with missing messages
. eliminate RCF_*GOLEM flags
. requirement.recycle is stupid hard
. set rules with -r
. test eressea installation uses correct rules, eressea.ini cleanup


Wayland Desktop on Pi
. http://wayland.freedesktop.org/xserver.html
